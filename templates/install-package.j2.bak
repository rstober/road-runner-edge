        

- name: update and upgrade software image {{ item['name'] }}
    ansible.builtin.shell: 'chroot /cm/images/{{ item['name'] }} /bin/bash apt update && apt full-upgrade'
    args:
      executable: /bin/bash
    register: cmsh_output
    ignore_errors: true
